// OpenNext Cloudflare adapter config.
// NOTE: the exact import path / options can differ slightly by installed version —
// we confirm against the installed @opennextjs/cloudflare during the deploy session.
import { defineCloudflareConfig } from "@opennextjs/cloudflare/config";

export default defineCloudflareConfig({});
